{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Git: Pull on folder open",
      "type": "shell",
      "command": "git pull --rebase --autostash",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [],
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
